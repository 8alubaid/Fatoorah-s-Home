// Web stub — the native Lean SDK (and react-native-webview) are never bundled
// for web. Web only ever runs the mock provider, so this renders nothing.
import React from "react";

export const LEAN_AVAILABLE = false;

export const LeanLink = React.forwardRef(function LeanLink(_props, _ref) {
  return null;
});

export default LeanLink;
