{
  "name": "fatoorah-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend for Fatoorah — holds the Lean Technologies secret and proxies Open Banking calls. The mobile app never holds the secret.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
