// Where "Sign in" / "Get started" send people. Set NEXT_PUBLIC_APP_URL in
// production (e.g. https://app.fatoorah.com); locally it's the Expo web app.
export const APP_URL = process.env.NEXT_PUBLIC_APP_URL ?? "http://localhost:8081";

export const CONTACT_EMAIL = "hello@fatoorah.app";
